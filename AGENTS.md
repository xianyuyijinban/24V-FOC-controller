# AGENTS.md — 可维护性编码 Agent SOP（Kimi Code 开箱即用版）

> 方法来源：B站「为什么你vibecoding的项目很难维护」（Esther不二 × 方格Fango）
> 用法：放进项目根目录 `AGENTS.md`（Kimi Code 会在会话开始时加载）。
> 子目录的 `AGENTS.md` 会覆盖父目录，适合 monorepo。
> 核心：**AI 改代码不碰坏别处，靠的是架构边界，不是运气。**

## 运行前（PRE-FLIGHT）—— 必须先答对这 3 问
1. 这个项目怎么分层？（先读 README / 目录结构 / 本文件）
2. 我要改的模块边界在哪？（属于哪些文件，依赖谁，谁依赖它）
3. 哪些是「锁定区」？（见下文，默认禁止改动）

答不上来 = 先读代码 + 问我，不要直接动手。范围广/风险高的改动，先开启 `/plan on` 规划模式。

## 锁定区（LOCKED ZONE）—— 默认禁止改动
以下模块不得擅自修改，除非我明确指令：
- 核心算法 / 底层框架 / 公共基础设施
- 已稳定、有测试兜底、正在生产运行的模块
- 与硬件/时序/外部接口强耦合的部分
- 我明确标注「别动」的文件

改动锁定区 = 必须先说明原因 + 等我确认。

## 改动中（DURING）—— 铁律
1. 小步改：一次只改一个逻辑点，不顺手重构一大片。
2. 给上下文：改之前先说明——要改什么、影响哪个模块、为什么。
3. 走接口：模块间用既有接口/入参通信，不新增全局可变状态，不直接改别处状态。
4. 复用：优先复用已有函数/组件，不复制粘贴重复逻辑。
5. 命名表意：新变量/函数/组件名见名知义，不用 `data` / `item` / `temp` / `tmp`。
6. 去魔法数：硬编码值抽成常量/配置，集中管理。
7. 不糊结构：不把新逻辑塞进「上帝文件」，归属到正确模块。

## 改动后（POST-FLIGHT）—— 必须自检再交付
1. 跑测试：有测试就跑（单元/集成/快照），确认没碰坏别处。
2. 看 diff：输出改动摘要（改了哪些文件、每处什么变化、影响范围）。
3. 回滚锚点：改动前确认有可回退基线（git commit/tag），说明如何 revert。
4. 自查清单：
   - 改一处是否只动相关模块？（牵连 3 处 = 架构有问题，报告不硬改）
   - 有没有新增全局状态 / 硬编码？
   - 命名是否表意？
   - 是否动了锁定区？（动了 = 明确告知）
5. 交付话术：给我简短摘要——改了什么、验证结果、如何回滚、下一步建议。

## 输出模板（每次改动完成时）
```
## 改动摘要
- 改了什么：<一句话>
- 涉及文件：<文件列表>
- 影响范围：<哪些模块可能受影响>

## 验证
- 测试结果：<通过/未跑/失败原因>
- 是否有锁定区改动：<无 / 有：需确认>

## 回滚
- 基线：<commit/tag>
- 回滚命令：<git revert ...>

## 下一步建议
- <可选>
```

## 快速判断（改崩预警）
> 改一个功能只动一个模块 → 正常，交付。
> 改一个功能牵连三个文件 → 停下，这是架构问题，先报告再重构，不要硬改。
> 动了锁定区 → 停下，先说明原因等确认。

## 本项目锁定区（FOC 固件 —— 禁止擅自修改）
- 电流环 / 速度环 / 位置环核心控制参数与 PID 结构
- 速度观测器（foc_observer）算法
- 编码器接口（TLE5012B）与角度解算
- PWM/SVPWM 输出与定时器配置（TIM1_UP 分频派生链路）
- 三相低边电流采样与 ADC 触发
- 已定版参数：kp 0.49 / kd 0.007 / ki 0.37 / 摩擦补偿 0.022
改动以上任何模块 = 先说明原因 + 等我确认。

## 备注
- 修改本文件提到的内容时，请同步更新本文件。
- 若需全局（所有项目）生效，可把核心规则放 `~/.kimi/AGENTS.md`。
